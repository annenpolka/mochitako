# mochitako の作業方針

## 辞書評価・編集データの外部送信

2026-09-11、ユーザーは以下の外部送信を明示的に許可した。

- 目的：このプロジェクトの辞書評価、候補語生成、編集方針の検討、モデル実験。
- 送信先：OpenCode経由のDeepSeek（`opencode-go/deepseek-v4.1-flash`を含む）およびDevin（`swe-2-max`を含む）。
- 対象データ：辞書、未公開の候補語・編集案、人間の選好回答、比較対象、同点・見送り・訂正履歴、回答に付随する理由や説明、それらの集計・編集資料。既存の250比較と、今後このプロジェクトで収集する同種のデータを含む。

上記の目的・送信先・データ範囲内では、実行のたびに送信許可を取り直す必要はない。
必要なデータをブリーフや添付資料に含めてよい。認証情報、秘密鍵、プロジェクトと無関係な個人情報はこの許可に含まれない。

この許可はモデルへの処理依頼のための送信に対するものであり、生回答や実行ログのGitへの追加・公開を許可するものではない。
`evaluation/results/` の原本・回答・生ログは引き続きgitignore対象として扱う。

## deep-researchレポートの保存

別途指示がなければ、原則として `~/Mechachang/raw/` 配下にMechachang側の命名規則に基づいて保存する。
